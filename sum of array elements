#include <stdio.h>
main() 
{  // a is array and i is counter
  int a[5], i;
  printf("Enter elements of array: ");
  for(i = 0; i < 5; i++)
  {
    scanf("%d", &a[i]);
    sum = sum + a[i];
  }
  printf("Sum of array elements= %d", sum);
} 
